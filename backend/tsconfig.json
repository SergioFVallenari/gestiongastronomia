{
  "compilerOptions": {
    "target": "ES6",
    "module": "commonjs",
    "outDir": "./dist",
    "rootDir": ".",  // Apunta a la raíz del proyecto
    "esModuleInterop": true,
    "strict": true,
    "skipLibCheck": true
  },
  "include": [
    "*.ts",        // Incluye archivos TypeScript en la raíz
    "src/**/*.ts"  // Incluye archivos TypeScript dentro de `src`
  ],
  "exclude": [
    "node_modules"
  ]
}